# Set GOTOOLCHAIN to local to prevent automatic Go version updates
export GOTOOLCHAIN=local

# Optional: Display the current setting when entering the directory
echo "GOTOOLCHAIN set to 'local' - Go version will remain at go.mod specification (1.23.0)"
